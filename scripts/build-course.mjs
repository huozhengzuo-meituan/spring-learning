import { readFile, writeFile, mkdir } from 'node:fs/promises';
import { fileURLToPath } from 'node:url';
import path from 'node:path';

const root = fileURLToPath(new URL('..', import.meta.url));
const escape = value => String(value).replace(/[&<>"']/g, ch => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[ch]));
const lessons = (await Promise.all(['part-1', 'part-2'].map(async part => JSON.parse(await readFile(path.join(root, 'course', `${part}.json`), 'utf8'))))).flat().sort((a, b) => a.id - b.id);
const slug = lesson => `${String(lesson.id).padStart(4, '0')}-${lesson.slug}.html`;
if (lessons.length !== 48 || lessons.some((l, i) => l.id !== i + 1)) throw new Error('Expected 48 consecutively numbered lessons.');
for (const lesson of lessons) {
  if (!/^[a-z0-9-]+$/.test(lesson.slug) || !lesson.goal || lesson.sections.length < 3 || !lesson.exercise.solution || lesson.sources.length < 1 || lesson.quiz.options.length !== 3 || ![0, 1, 2].includes(lesson.quiz.answer)) throw new Error(`Invalid lesson ${lesson.id}`);
}
const references = [
  ['glossary', '前后端术语对照', 'Bean、事务、分区、幂等与可观测性'],
  ['spring-map', 'Spring 生态与版本地图', '组件职责、版本升级、学习顺序'],
  ['data-transactions', '数据与事务速查', 'SQL、JPA、索引、并发控制'],
  ['redis-kafka', 'Redis 与 Kafka 速查', '命令、缓存边界、投递语义'],
  ['security-http', '接口与安全速查', '状态码、认证授权、CSRF、CORS'],
  ['operations', '测试与排障手册', '证据顺序、JVM、容器与发布'],
];
const head = (title, prefix = '') => `<!doctype html><html lang="zh-CN"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="color-scheme" content="light"><title>${escape(title)} · Spring 后端研习</title><link rel="stylesheet" href="${prefix}assets/course.css"><script src="${prefix}assets/course.js" defer></script></head>`;
const nav = prefix => `<a class="skip" href="#main">跳到正文</a><header class="topbar"><a class="brand" href="${prefix}index.html">SPRING / 后端研习</a><nav aria-label="主导航"><a href="${prefix}index.html#curriculum">全部课程</a><a href="${prefix}index.html#reference">速查手册</a><a href="${prefix}examples/README.md">项目说明</a></nav></header>`;
const footer = '<footer>Java 21 LTS · Spring Boot 4.1.1 · 内容核验于 2026-09-22。完成练习是学习证据的起点。</footer>';
const referenceDocs = JSON.parse(await readFile(path.join(root, 'course', 'references.json'), 'utf8'));
await mkdir(path.join(root, 'reference'), { recursive: true });
for (const ref of referenceDocs) {
  const content = `${head(ref.title, '../')}<body>${nav('../')}<main id="main" class="shell"><p class="eyebrow">Field reference / 可打印速查</p><h1>${escape(ref.title)}</h1><article class="reading">${ref.sections.map((s,i)=>`<section id="section-${i}"><h2>${escape(s.title)}</h2>${s.html}</section>`).join('')}<h2>官方依据</h2><ul class="source-list">${ref.sources.map(([title,url])=>`<li><a href="${escape(url)}">${escape(title)}</a></li>`).join('')}</ul><p><a href="../index.html#curriculum">回到课程练习 →</a></p></article></main>${footer}</body></html>`;
  await writeFile(path.join(root, 'reference', `${ref.slug}.html`), content);
}
await mkdir(path.join(root, 'lessons'), { recursive: true });
for (const [index, lesson] of lessons.entries()) {
  const sections = lesson.sections.map((s, i) => `<section id="section-${i}"><h2>${escape(s.title)}</h2>${s.html}</section>`).join('\n');
  const toc = lesson.sections.map((s, i) => `<li><a href="#section-${i}">${escape(s.title)}</a></li>`).join('');
  const html = `${head(lesson.title, '../')}<body data-lesson-id="${lesson.id}">${nav('../')}<main class="shell" id="main"><p class="eyebrow">Lesson ${String(lesson.id).padStart(2, '0')} / 48 · ${escape(lesson.phase)}</p><h1>${escape(lesson.title)}</h1><p class="intro">${escape(lesson.goal)}</p><div class="meta"><span class="tag">约 ${lesson.minutes} 分钟</span><span class="tag">先修：${escape(lesson.prerequisites.join(' / '))}</span><span class="tag">Java 21 · Boot 4.1</span></div><div class="reading-layout"><article class="reading"><div class="callout bridge"><strong>从你的前端经验出发</strong><p>${escape(lesson.bridge)}</p></div><p class="small">本课服务于同一个目标：独立交付并排查可靠的业务后端。实验入口：${escape(lesson.lab)}。标注为教学片段或设计实验的代码需要按题目补入，不代表示例已实现该功能。</p>${sections}<section id="recall"><h2>合上笔记，先判断</h2><form class="quiz" data-answer="${lesson.quiz.answer}" data-explanation="${escape(lesson.quiz.explanation)}"><fieldset><legend>${escape(lesson.quiz.question)}</legend>${lesson.quiz.options.map((option, i) => `<label><input type="radio" name="answer" value="${i}"><span>${escape(option)}</span></label>`).join('')}</fieldset><button type="submit">检查推理</button><div class="feedback" role="status" aria-live="polite"></div></form><noscript><p>即时反馈需要 JavaScript；可阅读下方静态答案。</p></noscript><details><summary>查看判断依据</summary><p>正确答案：${escape(lesson.quiz.options[lesson.quiz.answer])}。${escape(lesson.quiz.explanation)}</p></details></section><section id="practice"><h2>把知识变成操作</h2><p><strong>${escape(lesson.exercise.task)}</strong></p><ol>${lesson.exercise.steps.map(step => `<li>${escape(step)}</li>`).join('')}</ol><div class="callout"><strong>验收信号</strong><p>${escape(lesson.exercise.expected)}</p></div><details><summary>做完再看：参考答案与排错线索</summary><p>${escape(lesson.exercise.solution).replace(/\n/g, '<br>')}</p></details></section><section id="sources"><h2>回到一手资料</h2><ul class="source-list">${lesson.sources.map((source, i) => `<li><a href="${escape(source.url)}">${escape(source.title)}</a>${i === 0 ? ' · 本课首选精读' : ''}</li>`).join('')}</ul><p class="small">带着本课目标阅读相关章节，不要求通读整个参考手册。各知识段落的出处汇集于此。</p><p>有不清楚的地方，直接问我“第 ${lesson.id} 课的……为什么？”并贴上你的推理、代码或报错。我们可以一起拆解，并按证据更新学习记录。</p><p><a href="../reference/glossary.html">术语速查</a> · <a href="../reference/spring-map.html">生态地图</a> · <a href="../reference/operations.html">排错顺序</a></p></section><div class="review-panel"><strong>间隔回忆</strong><p data-review-date></p><div class="actions"><button type="button" data-practiced aria-pressed="false">我已完成本课练习</button><button type="button" class="secondary" data-export>导出练习记录</button></div><p class="small">仅记录自报实践，不代表已掌握。下次复习：先复述适用边界，再改动一个条件重新实验。</p><p class="status" data-status role="status"></p></div><nav class="lesson-nav" aria-label="课程前后页">${index ? `<a href="${slug(lessons[index - 1])}">← ${escape(lessons[index - 1].title)}</a>` : '<a href="../index.html">← 课程首页</a>'}${index < lessons.length - 1 ? `<a href="${slug(lessons[index + 1])}">${escape(lessons[index + 1].title)} →</a>` : '<a href="../index.html">返回课程首页 →</a>'}</nav></article><aside class="toc"><strong>本课导航</strong><ol>${toc}<li><a href="#recall">回忆测验</a></li><li><a href="#practice">动手练习</a></li><li><a href="#sources">官方资料</a></li></ol><a href="../index.html#curriculum">查看完整路线 ↗</a></aside></div></main>${footer}</body></html>`;
  await writeFile(path.join(root, 'lessons', slug(lesson)), html);
}
const phases = [...new Set(lessons.map(lesson => lesson.phase))];
const curriculum = phases.map(phase => `<section class="phase" data-phase><div class="phase-head"><h2>${escape(phase)}</h2><span class="small">${lessons.filter(lesson => lesson.phase === phase).length} 课</span></div><div class="lesson-grid">${lessons.filter(l => l.phase === phase).map(l => `<article class="lesson-card" data-card="${l.id}"><span class="lesson-number">${String(l.id).padStart(2, '0')}</span><div><h3><a href="lessons/${slug(l)}">${escape(l.title)}</a></h3><p>${escape(l.goal)}</p><p class="lesson-meta">${l.minutes} MIN · <span class="completed-label" data-completion>待练习</span></p></div></article>`).join('')}</div></section>`).join('');
const indexHtml = `${head('从前端到可靠的后端')}<body>${nav('')}<main class="shell" id="main"><section class="hero"><div><p class="eyebrow">A field guide for frontend engineers</p><h1>从接口，到系统。<br>把后端能力一层层建起来。</h1><p class="intro">以任务协作系统为主线，理解 Spring、数据库、Redis 与 Kafka 的工作边界。在可以运行的代码里练习，在故障里验证理解。</p><div class="meta"><span class="tag">Java 21 LTS</span><span class="tag">Spring Boot 4.1.1</span><span class="tag">48 课完整正文</span><span class="tag">2 个可运行项目</span></div><div class="actions"><a class="button" href="lessons/${slug(lessons[0])}">开始第 01 课 →</a><a class="button secondary" href="#curriculum">浏览学习路线</a></div></div><aside class="hero-note"><span class="big-number">48</span><p>每次学会一件事。<br>理解 → 回忆 → 实验 → 复盘</p><p class="small">先修：基本 Java 语法<br>建议：每周 3–4 课<br>主线约 12–16 周，项目练习另留时间。</p></aside></section><section><h2>你最终要能独立完成什么</h2><div class="diagram" aria-label="学习主线"><span>设计接口</span> → <span>守住数据</span> → <span>缓存与事件</span> → <span>观测与交付</span></div><p class="intro">不只写出 CRUD：解释一个请求如何被认证、如何提交事务、为什么消息会重复，以及系统出故障时先找什么证据。</p><div class="callout"><strong>项目随课程一起交付</strong><p><a href="examples/README.md">hello-api / task-service 运行手册</a>：前者聚焦 REST 与测试；后者默认用 H2 即可启动，进阶切换 PostgreSQL + Redis + Kafka。高级课程中的增量实验有明确任务与验收，并非所有生态组件都已集成到项目。</p><p><a href="SETUP.md">环境准备</a> · <a href="VERIFICATION.md">本次验证记录</a> · <a href="ROADMAP.md">阶段目标与毕业标准</a></p></div></section><section id="curriculum"><h2>完整学习路线</h2><p class="small">先按顺序建立基础；完成阶段后穿插复习前面课程。阅读时长不包含环境下载和完整实验。</p><p data-progress-count>0 / 48 课已练习</p><div class="progress-track" aria-hidden="true"><div class="progress-fill" data-progress-fill></div></div><div class="toolbar"><label>搜索课题<input type="search" data-search placeholder="例如：事务、Redis、Kafka" autocomplete="off"></label><label>练习状态<select data-filter><option value="all">全部课程</option><option value="todo">待练习</option><option value="done">已练习</option></select></label><button type="button" class="secondary" data-export>导出练习记录</button><span class="small" data-result-count role="status">显示 48 / 48 课</span></div><p data-status class="status" role="status"></p><noscript><p class="noscript">所有课文可直接阅读；筛选、测验与练习记录需要 JavaScript。</p></noscript>${curriculum}</section><section id="reference"><h2>留在手边的速查手册</h2><p class="small">用于日后查阅与打印。第一次学习请先看对应课文。</p><div class="refs">${references.map(([file, title, desc]) => `<a class="ref-card" href="reference/${file}.html"><strong>${title} ↗</strong><span>${desc}</span></a>`).join('')}</div><p><a href="RESOURCES.md">官方资料与实践社区</a> · <a href="MISSION.md">学习使命</a></p></section></main>${footer}</body></html>`;
await writeFile(path.join(root, 'index.html'), indexHtml);
const outline = lessons.map(l => `| ${String(l.id).padStart(2, '0')} | [${l.title}](lessons/${slug(l)}) | ${l.goal} | ${l.minutes} 分钟 |`).join('\n');
await writeFile(path.join(root, 'COURSE.md'), `# 完整课程目录\n\n48 课全部提供正文、回忆题、实验步骤、参考答案和官方资料。预计纯课文与短练习 ${Math.round(lessons.reduce((sum,l)=>sum+l.minutes,0)/60)} 小时；系统搭建、深入实验和毕业项目另计。主线建议 12–16 周，也可按证据调整。\n\n| 课次 | 主题 | 本课成果 | 建议时长 |\n| --- | --- | --- | --- |\n${outline}\n`);
const sourceMap = new Map();
for (const l of lessons) for (const s of l.sources) { const existing = sourceMap.get(s.url); if (existing) existing.lessons.push(l.id); else sourceMap.set(s.url, { ...s, lessons: [l.id] }); }
await writeFile(path.join(root, 'RESOURCES.md'), `# Spring 后端开发 Resources\n\n核验日期：2026-09-22。正文按官方资料综合编写；带 latest 的文档会变化，运行版本以项目 POM 和 compose 为准。\n\n## Knowledge\n\n${[...sourceMap.values()].map(s => `- [${s.title}](${s.url})\n  用于第 ${s.lessons.join('、')} 课。精读该课列出的概念与实验边界，遇到版本差异优先以固定依赖对应文档为准。`).join('\n')}\n\n## Wisdom (Communities)\n\n- [Spring 社区入口](https://spring.io/community)\n  用于寻找项目官方讨论、实践分享与贡献方式。带最小复现和版本信息提问。\n- [Apache Kafka 社区](https://kafka.apache.org/community/)\n  用于核对消息语义和运维实践；提交问题前附分区、消费组、offset与配置证据。\n- [Stack Overflow Spring 标签](https://stackoverflow.com/questions/tagged/spring)\n  用于比较具体问题的解决路径。回答年代不等于当前正确性，仍要用官方资料与实验复核。\n\n## Gaps\n\n- 本地单节点实验不能验证多副本容灾、真实流量容量或团队发布流程；毕业项目要求主动补齐对应证据。\n- 用户尚未表达社区参与偏好；参与是可选练习，不代用户发帖或联系他人。\n`);
console.log(`Built ${lessons.length} lessons, index, COURSE.md and ${sourceMap.size} curated source entries.`);
