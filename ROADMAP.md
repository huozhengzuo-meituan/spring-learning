# 从前端到后端的学习路线

本次已创建 48 课全部正文，不需要每次对话再生成下一课。课程覆盖后端主干；“Spring 全家桶”按职责与业务问题组织，不把每个历史项目都列成必修。

## 阶段与可观察成果

| 阶段 | 课次 | 交付成果 | 过关证据 |
| --- | --- | --- | --- |
| Java 与应用装配 | 01–08 | 可解释的 Boot 启动与依赖图 | 能说明版本、构造注入、单例共享、配置覆盖 |
| HTTP 与数据建模 | 09–16 | 有校验、错误契约与迁移的服务 | curl 正常/异常结果，SQL 模型和迁移记录 |
| 一致性与安全 | 17–24 | 并发安全、可回滚且有权限边界的 API | 回滚、409、401/403、CSRF、鉴权反例 |
| 缓存与消息基础 | 25–32 | 可观察的缓存与分区消费 | TTL、回源、key/partition、offset证据 |
| 异步一致性与可靠性 | 33–40 | 可重试的事件链路与排障方案 | 重复消费不重复副作用、outbox恢复、超时预算 |
| 系统设计与交付 | 41–48 | 有边界的模块和全栈毕业项目 | 契约、部署、演练记录与可解释取舍 |

建议每周 3–4 课，12–16 周完成主线；每次先回忆上次关键点。复杂实验和毕业项目另留 30–60 小时，实际速度按反馈调整。不存在要求用户必须遵守的进度期限。

## 一次学习的闭环

1. 不看课文，用 2 分钟解释上一课的边界。
2. 阅读本课，先预测代码或故障会发生什么，再运行。
3. 做测验与实验，保留命令、输出和自己的解释。
4. 对照答案；发现差异时定位到具体配置、调用路径或数据规则。
5. 第 1、3、7、14 天重复回忆，并改动一个条件做变式。

阅读结束不自动算掌握。只有你能解释错误答案为何错、能在改变条件后完成实验，才有足够证据调整学习记录。

## 两个现成项目与课程增量

- **hello-api**：零外部依赖的 REST 起点，聚焦构造注入、DTO、校验、错误和测试。
- **task-service**：默认 H2，切 profile 接 PostgreSQL、Redis、Kafka；完整任务状态机、乐观锁、角色与 CSRF、outbox 和消费去重。
- **课程增量**：OAuth2/OIDC、Testcontainers、WebFlux、Cloud、Batch、Integration、Kubernetes 等提供讲解、代码片段和实验任务；未声称这些全部已集成到现成应用。做完后形成你的独立作品。

不要先把一个服务拆成很多微服务。第42课先论证模块职责、事务边界、部署需求与团队成本；能说明为什么暂时不拆同样是合格成果。

## 毕业项目：可交付的任务协作系统

在现成项目上自行完成“用户/团队、任务协作、状态通知”闭环，并与熟悉的前端栈集成。加入真实身份提供方、资源归属校验、契约文档和环境隔离。提交：

- API 与事件契约、关系模型、至少两份设计取舍说明。
- 可重复运行的脚本、数据库迁移、前端关键路径。
- 业务测试、真实依赖集成、权限越界、重复消息、并发冲突证据。
- 日志/指标/trace定位一次故障的过程，负载与延迟统计的口径。
- Redis/Kafka短暂不可用的恢复证据、迁移兼容和部署回滚演练。

第48课提供100分评分表。建议80分为阶段目标；权限越界、数据不变量破坏或无法复现实验属于必须先补齐的缺口，不能靠文档分抵消。

## 主线之后

按工作场景选择深入：MySQL执行计划与InnoDB锁、Spring Data JDBC/R2DBC、Spring Session Redis、GraphQL、Kafka Streams/Connect、Debezium CDC、Resilience4j、Kubernetes多副本和中间件容量规划。Spring AI、Native Image、平台级源码研读可作为后续专题；是否加入由真实需求决定。
